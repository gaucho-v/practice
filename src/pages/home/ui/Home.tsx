import {BlockWithTitle, Layout} from "shared/ui";
import * as React from "react";

export const Home = () => {
    return (
        <Layout>
           <>
               <BlockWithTitle title={'Домашняя страница'}/>
           </>
        </Layout>
    )
}